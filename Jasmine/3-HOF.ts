// Write the function createAdder that works as described below:

// var addSix = createAdder(6);
// addSix(10); // --> 16
// addSix(21); // --> 27

// var addSeven = createAdder(7);
// addSeven(3); // --> 10

export default function(fixedAmountToAdd: number) {
  return null;
}
